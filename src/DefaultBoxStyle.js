/**
 * @flow
 */

const defaultBoxStyle = {
  position: 'relative',

  margin: 0,
  padding: 0,

  display: 'flex',
  alignItems: 'stretch',
  flexBasis: 'auto',
  flexShrink: 0,

  minHeight: 0,
  minWidth: 0,
};

export default defaultBoxStyle;
